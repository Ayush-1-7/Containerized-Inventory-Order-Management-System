# Environment
.env
.env.*
!.env.example
# Non-secret, public production API base URL — safe to commit so the Vercel
# build always bakes it in (a dashboard env var still overrides it).
!frontend/.env.production

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.sqlite3
*.db
.coverage
htmlcov/

# Node / frontend
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-error.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local tooling (Claude Code launch + local settings)
.claude/
